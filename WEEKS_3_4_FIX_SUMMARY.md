# إصلاح مشكلة الأسابيع 3 و 4 في المرحلة الأولى

## المشكلة
الأسابيع 3 و 4 موجودة في ملف `PlanData.json` ولكن لا تظهر في المرحلة الأولى في التطبيق.

## سبب المشكلة
البيانات يتم تحميلها من قاعدة البيانات المحلية (IndexedDB) وليس مباشرة من ملف JSON. إذا كانت قاعدة البيانات فارغة أو لا تحتوي على جميع الأسابيع، فلن تظهر الأسابيع المفقودة.

## الإصلاحات المطبقة

### 1. تحسين صفحة أسابيع المرحلة (PhaseWeeksPage.tsx)
- تم تعديل طريقة عرض أسابيع المرحلة لضمان ظهور جميع الأسابيع المحددة في المرحلة
- إذا لم يتم العثور على الأسبوع في الخطة المحملة، يتم إنشاء أسبوع مؤقت
- إضافة فحص شامل لجميع الأسابيع المحددة في المرحلة

### 2. تحسين صفحة المراحل (PhasesPage.tsx)
- إضافة تسجيل تفصيلي لتتبع الأسابيع المفقودة
- إضافة زر "تحديث البيانات" لتحديث البيانات من الملف الأصلي
- تحسين حساب تقدم المرحلة

### 3. تحسين AppContext.tsx
- إضافة فحص لضمان تحميل جميع أسابيع الخطة
- إضافة إعادة تحميل تلقائية إذا كانت بعض الأسابيع مفقودة
- تحسين دالة `refreshData` لضمان اكتمال البيانات

### 4. التحسينات التقنية
- إضافة فحص شامل لجميع الأسابيع المحددة في `phases.json`
- إضافة تسجيل تفصيلي للمساعدة في تتبع المشاكل
- تحسين معالجة الأخطاء

## الملفات المعدلة

1. `src/components/phases/PhaseWeeksPage.tsx`
   - تعديل طريقة عرض أسابيع المرحلة
   - إضافة إنشاء أسابيع مؤقتة إذا لم توجد في الخطة

2. `src/components/phases/PhasesPage.tsx`
   - إضافة تسجيل تفصيلي
   - إضافة زر تحديث البيانات
   - تحسين حساب تقدم المرحلة

3. `src/context/AppContext.tsx`
   - إضافة فحص اكتمال البيانات
   - تحسين دالة `refreshData`
   - إضافة إعادة تحميل تلقائية

## النتائج المتوقعة

✅ الأسابيع 3 و 4 ستظهر في المرحلة الأولى
✅ جميع الأسابيع المحددة في `phases.json` ستظهر في مراحلها
✅ إمكانية تحديث البيانات يدوياً إذا لزم الأمر
✅ تسجيل تفصيلي للمساعدة في تتبع المشاكل

## كيفية الاستخدام

1. **التحديث التلقائي**: سيتم تحديث البيانات تلقائياً عند تحميل التطبيق
2. **التحديث اليدوي**: يمكن الضغط على زر "تحديث البيانات" في صفحة المراحل
3. **مراقبة التسجيل**: يمكن مراقبة console للتحقق من الأسابيع المفقودة

## ملاحظات

- الأسابيع المؤقتة ستظهر بعناوين بسيطة حتى يتم تحميل البيانات الكاملة
- زر التحديث مفيد في حالة عدم اكتمال البيانات الأولية
- التسجيل التفصيلي يساعد في تتبع أي مشاكل مستقبلية