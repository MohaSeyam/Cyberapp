# إصلاح مشكلة الأسابيع 3 و 4 المفقودة

## المشكلة:
كانت الأسابيع 3 و 4 لا تظهر في التطبيق رغم وجودها في ملف البيانات.

## الحلول المطبقة:

### 1. تحسين تحميل البيانات في AppContext
- إضافة فحص شامل للأسابيع المفقودة
- إعادة تحميل البيانات تلقائياً إذا كانت مفقودة
- إضافة رسائل تأكيد للمستخدم

### 2. إضافة وظيفة forceReloadData
- وظيفة جديدة لإعادة تحميل البيانات بشكل قوي
- مسح البيانات القديمة من IndexedDB
- استيراد البيانات الجديدة من PlanData.json
- التحقق من وجود جميع الأسابيع

### 3. تحديث صفحة الإعدادات
- إضافة زر لحل مشكلة الأسابيع 3 و 4
- استخدام الوظيفة الجديدة forceReloadData
- رسائل واضحة للمستخدم

## الملفات المحدثة:

1. **src/context/AppContext.tsx**
   - إضافة وظيفة forceReloadData
   - تحسين فحص الأسابيع المفقودة
   - إضافة رسائل تأكيد

2. **src/pages/SettingsPage.tsx**
   - تحديث وظيفة handleClearAndReload
   - إضافة forceReloadData إلى useApp
   - تحسين رسائل المستخدم

## كيفية الاستخدام:

1. **الطريقة التلقائية:**
   - التطبيق يفحص تلقائياً الأسابيع المفقودة
   - يعيد تحميل البيانات إذا كانت مفقودة

2. **الطريقة اليدوية:**
   - اذهب إلى صفحة الإعدادات
   - اضغط على زر "حل مشكلة الأسبوع 3 و 4"
   - تأكيد العملية

## التحقق من الإصلاح:

- الأسابيع 3 و 4 تظهر الآن في المرحلة الأولى
- جميع الأسابيع من phases.json موجودة في التطبيق
- رسائل تأكيد واضحة للمستخدم

## ملاحظات:
- البيانات الأصلية كانت موجودة في PlanData.json
- المشكلة كانت في تحميل البيانات من IndexedDB
- الإصلاح يضمن تحميل جميع الأسابيع بشكل صحيح